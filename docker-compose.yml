version: '3.8'

services:
  my-web:
    build: .
    ports:
      - "9090:80"
    restart: always

  my-db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: mypassword
    # تلاحظ أننا لم نضع ports لـ Postgres لأننا نريد حمايتها داخل الشبكة فقط
