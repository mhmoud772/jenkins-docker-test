version: '3.8'

services:
  web-dev:
    build: .
    ports:
      - "9001:80"
    restart: always
    
  my-db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: mypassword
    volumes:    
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: 
